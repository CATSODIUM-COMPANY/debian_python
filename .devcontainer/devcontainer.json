{
	"name": "New Debian docker",
	"forwardPorts": [8000],
	"build": {
		"dockerfile": "Dockerfile" ,
		"args" : {
			"MY_WORKDIR": "/workspaces/debian_python"
		}
	},
	"workspaceFolder": "/workspaces/debian_python",
	"postStartCommand": "rm -rf .devcontainer"
}
