{
	"name": "New Debian docker",
	"forwardPorts": [8000],
	"build": {
		"dockerfile": "Dockerfile" 
	},
	"workspaceFolder": "/workspaces/debian_python",
	"postStartCommand": "chmod +x ./src/grader_latest; rm -rf .devcontainer"
}
